function setup() {
  createCanvas(1000, 1000);
  background(0,255,0);
  strokeWeight(10);
  stroke(0,0,255);
  line(340,200,200,200);
  line(200,200,210,600)
  stroke(255,0,0);
  point(400,200);
}
// Intended Purpose:
// This is supposed to show what the SNAKE game looks like when paused (stationary).